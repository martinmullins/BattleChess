{
  "name": "battle-chess",
  "version": "0.1.0",
  "description": "Battle Chess — TypeScript browser port of the MS-DOS classic",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.build.json && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "jsdom": "^29.0.1",
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vitest": "^4.1.1"
  }
}
